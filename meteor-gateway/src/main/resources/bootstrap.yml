# Tomcat
server:
  port: 8080
# 日志配置
logging:
  config: classpath:logback-spring.xml
  file:
    path: logs
# Spring
spring:
  application:
    # 应用名称
    name: gateway-server
  profiles:
    # 环境配置
    active: local
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 127.0.0.1:8848
      config:
        # 配置中心地址
        server-addr: 127.0.0.1:8848
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "http://example.com"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
            allowedHeaders: "*"
            allowCredentials: true
      discovery:
        locator:
          lowerCaseServiceId: true
          enabled: true
      routes:
        # 认证中心
        - id: meteor-auth-api
          uri: lb://auth-server
          predicates:
            - Path=/auth/**
          filters:
            - RewritePath=/auth/v3/api-docs, /v3/api-docs
        # 基础设施服务
        - id: shared-admin-api
          uri: lb://shared-server
          predicates:
            - Path=/admin-api/shared/**
          filters:
            - RewritePath=/shared/v3/api-docs, /v3/api-docs
        # 系统模块
        - id: system-admin-api
          uri: lb://system-server
          predicates:
            - Path=/admin-api/system/**
          filters:
            - RewritePath=/system/v3/api-docs, /v3/api-docs
        # 支付中心
        - id: meteor-pay-api
          uri: lb://pay-server
          predicates:
            - Path=/pay/**

knife4j:
  # 聚合 Swagger 文档，参考 https://doc.xiaominfo.com/docs/action/springcloud-gateway 文档
  gateway:
    enabled: true
    routes:
      - name: auth-server
        service-name: auth-server
        url: /auth/v3/api-docs
      - name: shared-server
        service-name: shared-server
        url: /admin-api/shared/v3/api-docs
      - name: system-server
        service-name: system-server
        url: /admin-api/system/v3/api-docs
