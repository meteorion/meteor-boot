package pers.meteor.pay.domain.channel.event;

import pers.meteor.event.core.entity.DomainEventType;

/**
 * @author meteor
 */
public enum ChannelConfigEventType implements DomainEventType {
   UPDATED
}
